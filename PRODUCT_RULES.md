# 资深产品规则

版本：V1.0｜适用：Web、SaaS、电商、管理后台、API、移动端、AI 产品

## 1. 适用与引用

本文件定义业务目标、范围、角色、流程、状态和验收标准。代码实现遵循 [`DEVELOPMENT_RULES.md`](./DEVELOPMENT_RULES.md)，视觉与交互遵循 [`UI_RULES.md`](./UI_RULES.md)，验证遵循 [`TEST_RULES.md`](./TEST_RULES.md)，AI 执行遵循 [`AI_AGENT_RULES.md`](./AI_AGENT_RULES.md)。

规则冲突时，遵循 `AI_AGENT_RULES.md` 规定的项目优先级；产品规则不能降低安全、资金、权限和数据一致性要求。

## 2. 核心原则

- 先理解用户问题，再设计功能；功能必须有明确业务价值。
- 需求必须描述用户、场景、目标、边界、正常流程、异常流程、数据变化和验收方式。
- 不以“和某网站一样”“开发自己看着办”“异常以后再说”作为需求。
- 代码写完不等于功能完成；以 Definition of Done 为准。

## 3. 需求模板

每个需求必须包含：

- 用户角色：游客、注册用户、会员、商家、管理员、API 调用方等。
- 用户故事：当 `[角色]` 在 `[场景]` 下，希望 `[目标]`，从而 `[价值]`。
- 范围：包含什么、不包含什么、依赖什么。
- 权限：谁能对什么资源执行什么操作，资源归属如何判断。
- 数据：来源、所有者、创建/更新/删除条件、保留期限、审计要求、敏感等级。
- 状态机：状态、进入条件、允许操作、是否可回退、触发动作和通知。
- 异常：不存在、重复、超时、无权限、登录过期、刷新、返回、并发、第三方故障。
- Given / When / Then 验收标准。
- 埋点、日志、监控、客服与运营需要（如适用）。

## 4. 状态与流程

有状态变化的业务必须使用明确枚举和状态转移，不用互相冲突的 Boolean 拼接复杂状态。示例订单：`PENDING → PAID → PROCESSING → SHIPPED → COMPLETED`；异常可包括 `FAILED`、`CANCELLED`、`REFUNDING`、`REFUNDED`。

每次状态变更必须定义操作者、前置状态、幂等键、时间记录、审计日志、通知、回滚/补偿方式。前端展示状态不代表服务端已完成状态变更。

## 5. CRUD 与表单

Create 定义必填、格式、长度、默认值、重复提交和重复数据；Read 定义 Loading、Empty、Error、分页、搜索、排序、筛选和权限；Update 定义并发修改、数据不存在和失败恢复；Delete 定义软/硬删除、关联数据、恢复方式、确认方式和权限。高价值数据默认不可物理删除。

表单必须设计 Label、Placeholder、帮助文本、必填标识、输入限制、字段级错误、提交 Loading/Disabled、成功反馈和失败恢复。错误文案必须可行动，例如“邮箱格式不正确”，不能只写“操作失败”。

## 6. 专项业务要求

### 支付与订单

支付需求必须明确订单金额、币种、订单状态、支付记录、支付方、超时、取消、退款、Webhook、签名、幂等和对账。订单金额不能由客户端决定；支付成功必须以可信服务端确认/验证为准。禁止重复扣款、重复发货或“页面显示成功但数据库未确认”。

### 订阅与权益

明确试用、激活、续费、失败重试、暂停、取消、生效时间、到期时间、升级/降级、按比例计费、退款、宽限期和权益快照。订阅状态与支付状态分离建模；Webhook 延迟或乱序不能导致权益错误。

### 邮件与通知

明确触发事件、收件人、模板版本、语言、退订规则、重试、发送状态、去重键和失败补偿。密码、Token、完整支付信息等敏感数据不得进入邮件或日志。

### 数据库变更

需求必须说明新增/修改字段、历史数据迁移、默认值、空值策略、索引/唯一约束、兼容旧版本、回滚与发布顺序。任何线上执行过的 Migration 不得直接修改，应新增向前兼容的 Migration。

## 7. 开发前产品检查清单

- [ ] 用户角色、资源归属和权限已明确
- [ ] 范围与非目标已明确
- [ ] 状态机和所有合法转移已明确
- [ ] 正常、异常、边界、并发流程已明确
- [ ] 数据模型、审计、保留与删除策略已明确
- [ ] 支付/订单/订阅/邮件/数据库影响已评估
- [ ] UI 状态与无障碍要求已交给设计/开发
- [ ] Given / When / Then 验收标准已完成
- [ ] 发布、监控、回滚与客服处理方案已明确

## 8. 产品 Definition of Done

需求文档、验收标准、权限、状态、异常、数据影响和发布风险均已评审；实现通过 [`TEST_RULES.md`](./TEST_RULES.md) 的相关测试，产品验收覆盖正常与失败路径；无未记录的范围变更、假数据或未决 P0/P1 问题。
