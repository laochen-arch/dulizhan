# 资深开发规则

版本：V1.0

## 1. 适用与引用

本文件约束架构、代码、数据库、API、安全、发布和审查。业务输入来自 [`PRODUCT_RULES.md`](./PRODUCT_RULES.md)，UI 约束见 [`UI_RULES.md`](./UI_RULES.md)，测试要求见 [`TEST_RULES.md`](./TEST_RULES.md)，AI 执行约束见 [`AI_AGENT_RULES.md`](./AI_AGENT_RULES.md)。

## 2. 工程优先级

正确性、安全、数据完整性、可靠性、可维护性、性能、开发速度。不得以“先上线”“先能跑”为理由跳过权限、事务、校验、测试或回滚。

## 3. 开发前检查

- [ ] 阅读相关目录、调用链、数据模型、API、配置和现有测试
- [ ] 确认需求边界、状态机、权限与验收标准
- [ ] 找出影响范围、兼容性风险和回滚方案
- [ ] 识别是否涉及资金、个人数据、数据库、第三方或并发
- [ ] 选择最小可行修改方案，记录必要的架构决策

禁止猜字段、API、环境变量、Schema 或业务规则；不因一个局部问题重写整页、整服务或整套架构。

## 4. 代码规范

- 命名表达业务含义；避免 `data`、`temp`、`obj`、`result2` 等模糊名称。
- 函数单一职责；UI、业务、数据访问、外部服务和副作用分层。
- 不创建万能 `utils.ts`、`service.ts`；按领域、功能和责任拆分。
- 重复两次先观察，重复三次再抽象；不得为 DRY 制造过度抽象。
- 不用空 catch、无理由 `@ts-ignore`、大面积 `any`、删除测试或关闭检查掩盖问题。
- API 必须有输入校验、认证授权、明确状态码、标准错误结构、日志、超时和必要的幂等键。
- 核心校验必须在服务端；前端隐藏按钮不是权限控制。

统一错误结构示例：

```json
{"success":false,"error":{"code":"ORDER_NOT_FOUND","message":"Order does not exist"}}
```

## 5. 数据库与数据完整性

任何 Schema 变更必须评估主键、外键、唯一约束、索引、空值、默认值、级联、历史数据、锁表风险、向前/向后兼容和回滚。线上 Migration 只追加，不修改历史文件；大表变更采用分阶段、可观测、可中止方案。

涉及订单、支付、退款、库存、余额、优惠券、积分或权益时，使用事务、唯一约束、原子更新或锁；不能仅依赖“先 SELECT 再 UPDATE”。金额使用最小货币单位整数或 Decimal，禁止浮点金额。

## 6. 支付、订单、订阅、邮件

- 支付：服务端计算金额并验证支付结果、币种、订单归属和签名；处理超时、取消、失败、退款、重复回调和对账。
- 订单：状态转移必须受服务端约束；创建订单、扣库存、支付记录和权益发放必须保证一致性或有明确补偿。
- 订阅：支付状态、订阅状态、权益状态分离；升级、降级、试用、宽限期、取消、续费失败和 Webhook 乱序需可重放、可审计。
- 邮件：异步发送，记录模板版本和发送状态；使用幂等键、退避重试、死信/补偿；不把发送成功冒充业务成功。
- 第三方：设置超时、重试上限、Rate Limit 处理、签名校验、熔断/降级和监控；重试必须避免重复副作用。

## 7. 安全与日志

所有输入不可信，防范 SQL Injection、XSS、CSRF、SSRF、命令注入、路径穿越、开放重定向、文件上传攻击和 IDOR。Secret 只放环境变量/Secret Manager，不提交 Git。生产日志不得包含密码、Token、Cookie、银行卡、私钥或完整身份证信息；记录可关联的 request ID、错误码和业务 ID。

## 8. 前端与性能

组件区分 UI、业务、页面、Hook、Service 和状态；异步状态至少覆盖 Idle、Loading、Success、Empty、Error、Disabled，并处理竞态、取消和重复提交。性能优化基于测量，重点检查 N+1、全表扫描、无分页、无索引、重复请求、巨型 Bundle 和内存泄漏。

## 9. Git、Review 与发布

Commit 使用 `feat:`、`fix:`、`refactor:`、`test:`、`docs:`、`chore:`、`perf:`，表达真实目的。Review 必查业务、权限、错误处理、并发、数据、安全、性能、测试、Secret 和无关改动。

发布前确认 Build、Type Check、Lint、测试、Migration、环境变量、第三方配置、监控、备份和回滚；生产问题按“止损→定位→恢复→根因修复→复盘”处理。禁止无审计手工改关键数据。

## 10. 完成后测试清单

- [ ] 相关 Unit、Integration、API、E2E 已运行
- [ ] 正常、异常、权限、并发、刷新、重试、超时已验证
- [ ] 支付/订单/订阅/邮件/数据库专项场景已验证（如适用）
- [ ] Type Check、Lint、Build 通过
- [ ] Migration 可执行、可观测、可回滚或有补偿
- [ ] 无 P0/P1、无假实现、无敏感日志、无 Secret

## 11. Definition of Done

实现、Review、测试、文档、监控、发布与回滚方案全部完成；变更范围与需求一致；任何已知风险已记录并获明确接受。代码写完不等于 Done。
