# 资深测试规则

版本：V1.0

## 1. 适用与引用

本文件定义测试策略、风险、用例、缺陷和发布门禁。测试输入来自 [`PRODUCT_RULES.md`](./PRODUCT_RULES.md)，实现质量依据 [`DEVELOPMENT_RULES.md`](./DEVELOPMENT_RULES.md)，视觉和无障碍依据 [`UI_RULES.md`](./UI_RULES.md)，AI 必须执行 [`AI_AGENT_RULES.md`](./AI_AGENT_RULES.md) 的验证要求。

## 2. 测试原则

测试目标是发现用户损失、业务错误、数据异常、安全问题和系统崩溃，不是只点一遍 Happy Path。每个功能至少覆盖正常、异常、边界、权限、安全、并发、网络、兼容、数据和恢复。

## 3. 测试层级与输入

优先使用 Unit、Integration、API、E2E 分层验证，避免所有问题都依赖 E2E。输入覆盖正常、最小、最大、越界、空、Null、前后空格、特殊字符、HTML/JS/SQL 字符、Unicode、Emoji 和超长值。数量范围为 1–100 时至少测试 0、1、2、99、100、101。

## 4. 核心场景

- 权限：A 是否能访问/修改 B 的资源；修改 URL ID 是否越权；普通用户是否能调用管理员 API。
- 网络：慢网、离线、超时、中断、重试、重复请求、刷新、前进后退、关闭重开。
- 数据：UI、API、数据库、第三方四者是否一致；失败后是否留下半完成数据。
- UI：Loading、Empty、Error、Disabled、Focus、键盘、响应式和移动端安全区。
- 兼容：当前 Chrome、Safari、Edge、Firefox，以及 iOS Safari、Android Chrome（按项目范围执行）。

## 5. 支付/订单/订阅/邮件专项

支付必须测试成功、失败、取消、超时、重复支付、重复/延迟/乱序 Webhook、金额/币种不一致、签名错误、退款和重复退款。订单测试重复创建、并发购买、库存不足、状态非法回退、刷新恢复和重复发货。订阅测试试用、激活、续费、失败重试、宽限期、取消、到期、升级/降级、退款及权益一致性。邮件测试模板、语言、退订、重复触发、重试、发送失败、模板版本和敏感信息泄露。

## 6. 数据库变更测试

在代表性旧数据上验证 Migration、默认值、空值、索引/唯一约束、向前/向后兼容、锁表/性能和回滚或补偿。验证新旧应用版本交替部署时不会丢数据；不得只验证“Migration 命令成功”。

## 7. 并发与安全

测试库存只剩 1 件的同时购买、单订单同时支付、优惠券最后一张、并发退款、管理员同时修改。验证 SQL Injection、XSS、CSRF、SSRF、路径穿越、上传、开放重定向、IDOR、认证过期和权限绕过；日志不得泄露 Secret。

## 8. 缺陷与发布门禁

P0：不可用、严重数据损坏、重大资金/安全事故，禁止发布。P1：核心流程、订单或关键权限错误，原则上禁止发布。P2：重要功能明显问题，需评估。P3：低影响体验问题，可排期。缺陷报告必须含环境、版本、前置条件、步骤、预期、实际、证据、日志、严重级别和复现率。

## 9. 完成后测试清单

- [ ] 需求验收标准逐项通过
- [ ] 正常、异常、边界、权限、并发、网络、恢复通过
- [ ] 支付/订单/订阅/邮件/数据库专项通过（如适用）
- [ ] Unit、Integration、API、关键 E2E 通过
- [ ] Type Check、Lint、Build 通过
- [ ] 浏览器、移动端、响应式和无障碍检查通过
- [ ] 无 P0/P1；已知风险有记录、负责人和处理决定
- [ ] 测试证据、数据清理、回归范围已记录

## 10. Definition of Done

测试不是“没有发现问题”而是风险已被覆盖并有证据。只有验收标准通过、回归完成、专项风险处理、发布门禁满足且缺陷状态明确，功能才可标记 Done。
