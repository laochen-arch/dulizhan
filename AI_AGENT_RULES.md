# AI Agent 编码规则

版本：V1.0｜适用于 Cursor、Claude Code、Codex、Windsurf 等编码 Agent

## 1. 规则关系与冲突优先级

本文件是 AI 执行入口；业务事实引用 [`PRODUCT_RULES.md`](./PRODUCT_RULES.md)，代码约束引用 [`DEVELOPMENT_RULES.md`](./DEVELOPMENT_RULES.md)，UI 引用 [`UI_RULES.md`](./UI_RULES.md)，验证引用 [`TEST_RULES.md`](./TEST_RULES.md)。规则冲突按以下顺序处理：

`用户数据安全 > 资金安全 > 权限安全 > 数据一致性 > 业务正确性 > 系统稳定性 > 用户体验 > 可维护性 > 性能 > 开发速度 > 代码漂亮程度`。

若需求与高优先级规则冲突，必须停下并报告风险，不得默默选择危险实现。若信息不足，先检查项目事实；无法确认时明确假设，不得猜测。

## 2. 强制工作流

每个任务必须执行：

1. Understand：复述目标、范围、非目标和验收标准。
2. Inspect：阅读目录、相关代码、配置、Schema、API、调用方和测试。
3. Trace：追踪数据流、状态流、权限链和外部副作用。
4. Identify：确定根因、影响范围、风险和回滚方案。
5. Design：提出最小、可靠、可测试的方案。
6. Implement：只修改必要文件，保留既有行为。
7. Review：检查安全、权限、并发、错误、日志、边界和无关改动。
8. Test：运行相关检查与测试。
9. Verify：验证真实业务结果和数据库/第三方一致性。
10. Report：说明改了什么、验证了什么、未验证什么和剩余风险。

## 3. 最小修改与边界

禁止因修按钮重写页面、因修接口重构服务、因类型错误关闭严格检查、因测试失败删除测试。发现无关问题时记录即可；只有其阻塞当前任务或造成安全/数据风险时才处理。不得擅自改需求、换架构、删除历史功能、改 UI 风格或扩大范围。

## 4. 禁止假完成

禁止 TODO、空函数、假按钮、静态数据冒充 API、Mock 冒充真实集成、捕获错误后返回成功、未完成却返回 `{success:true}`、绕过权限、跳过测试或隐藏错误。不能验证的内容必须诚实标记为未验证。

## 5. 数据库与高风险业务

数据库变更前必须检查当前 Schema、历史 Migration、旧数据、索引、约束、兼容和回滚；线上 Migration 不得修改，只能新增。涉及资金、订单、库存、订阅、权益、邮件时，必须检查事务/补偿、金额精度、幂等、签名、重试、Webhook 重放/乱序、并发和审计。不得把前端状态当作支付成功，不得重复扣款、重复发货、重复退款或重复发权益。

邮件必须有真实触发条件、去重键、模板版本、发送状态和失败补偿；数据库和第三方调用失败时不能假装业务完成。敏感数据不进日志、邮件、提交记录或测试样例。

## 6. 编码约束

- 先复用现有模式、组件、服务和错误结构，再创建新抽象。
- 所有输入服务端校验；权限在服务端执行；关键写操作有幂等或唯一约束。
- 不使用空 catch、无理由 `@ts-ignore`、大面积 `any`、关闭 Lint/TypeScript、删除断言或测试。
- 不提交 Secret；不输出密码、Token、Cookie、银行卡或私钥。
- 异步 UI 覆盖 Idle/Loading/Success/Empty/Error/Disabled，并防重复点击与竞态。

## 7. 开发前清单

- [ ] 已找到真实实现、调用链、数据模型、配置和测试
- [ ] 已确认角色、权限、状态机、验收标准和非目标
- [ ] 已评估支付/订单/订阅/邮件/数据库/第三方影响
- [ ] 已识别并发、幂等、事务、回滚和数据迁移风险
- [ ] 已确定最小修改文件与验证命令
- [ ] 不存在必须猜测的字段、接口、密钥或业务规则

## 8. 完成后清单

- [ ] 正常、异常、边界、权限、并发、刷新、超时和重试已验证
- [ ] 相关 Unit/Integration/API/E2E 已运行
- [ ] Type Check、Lint、Build 已运行并通过
- [ ] 支付/订单/订阅/邮件/数据库专项已验证（如适用）
- [ ] UI 状态、响应式、无障碍和错误文案已检查
- [ ] 无假数据、无假成功、无敏感日志、无 Secret、无 P0/P1
- [ ] 变更可解释、可回滚或有补偿；文档已同步

## 9. 强制交付报告

每次任务结束必须输出：

```text
修改内容：
- 文件与行为变化

验证结果：
- 已执行的检查/测试及结果

未验证内容：
- 因环境、凭据或外部服务限制未执行的项目

风险与后续：
- 已知风险、回滚/补偿方式、建议后续任务
```

## 10. Definition of Done

AI 只有在实现、审查、测试、业务验证和交付报告均完成时才可声明 Done。不能用“代码看起来正确”“编译通过”或“页面能打开”替代真实验证。
